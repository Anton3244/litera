// Налаштування, спільні для всіх пристроїв.

/**
 * Адреса сервера синхронізації — свій Cloudflare Worker.
 * Розгорни його за інструкцією sync/README.md і встав адресу сюди —
 * тоді на другому пристрої достатньо буде ввести код, без жодних адрес.
 *
 * Приклад: 'https://litera-sync.твій-акаунт.workers.dev'
 */
export const SYNC_URL = 'https://litera-sync.antony14026002.workers.dev';

/**
 * Відбиток пароля до режиму створювача (#/dev). Сам пароль тут не зберігається.
 *
 * Це замок від випадкового заходу, а не захист: застосунок виконується
 * в браузері, тож той, хто відкриє початковий код, обійде перевірку.
 * Нічого таємного за цими дверима й немає — лише інструменти для налагодження.
 *
 * Щоб змінити пароль, виконай у консолі браузера:
 *   crypto.subtle.digest('SHA-256', new TextEncoder().encode('новий-пароль'))
 *     .then(b => console.log([...new Uint8Array(b)]
 *       .map(x => x.toString(16).padStart(2, '0')).join('')))
 * і встав отриманий рядок сюди.
 */
export const DEV_PASS_HASH = '84d1922c38dab1245b5981ce6f256aebd081a99c36115050b223da4631dccd40';
